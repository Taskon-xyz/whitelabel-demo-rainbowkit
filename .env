# Environment variables are now split by environment.
# - .env.test is used for testnet builds and development.
# - .env.production is used for production builds.
# The default dev/build scripts load the correct file for you.
# For local overrides, copy one of the above to .env.local and adjust.
